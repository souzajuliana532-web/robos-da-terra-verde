// ===== TIMEZONE DATA =====\nconst TIMEZONES = [\n    { name: 'New York', timezone: 'America/New_York', offset: 'UTC-5/-4' },\n    { name: 'Los Angeles', timezone: 'America/Los_Angeles', offset: 'UTC-8/-7' },\n    { name: 'Chicago', timezone: 'America/Chicago', offset: 'UTC-6/-5' },\n    { name: 'Denver', timezone: 'America/Denver', offset: 'UTC-7/-6' },\n    { name: 'São Paulo', timezone: 'America/Sao_Paulo', offset: 'UTC-3/-2' },\n    { name: 'Buenos Aires', timezone: 'America/Argentina/Buenos_Aires', offset: 'UTC-3' },\n    { name: 'Londres', timezone: 'Europe/London', offset: 'UTC+0/+1' },\n    { name: 'Paris', timezone: 'Europe/Paris', offset: 'UTC+1/+2' },\n    { name: 'Berlim', timezone: 'Europe/Berlin', offset: 'UTC+1/+2' },\n    { name: 'Moscou', timezone: 'Europe/Moscow', offset: 'UTC+3' },\n    { name: 'Dubai', timezone: 'Asia/Dubai', offset: 'UTC+4' },\n    { name: 'Tóquio', timezone: 'Asia/Tokyo', offset: 'UTC+9' },\n    { name: 'Hong Kong', timezone: 'Asia/Hong_Kong', offset: 'UTC+8' },\n    { name: 'Singapura', timezone: 'Asia/Singapore', offset: 'UTC+8' },\n    { name: 'Bangkok', timezone: 'Asia/Bangkok', offset: 'UTC+7' },\n    { name: 'Istambul', timezone: 'Europe/Istanbul', offset: 'UTC+3' },\n    { name: 'Sydney', timezone: 'Australia/Sydney', offset: 'UTC+10/+11' },\n    { name: 'Melbourne', timezone: 'Australia/Melbourne', offset: 'UTC+10/+11' },\n    { name: 'Auckland', timezone: 'Pacific/Auckland', offset: 'UTC+12/+13' },\n    { name: 'Fiji', timezone: 'Pacific/Fiji', offset: 'UTC+12/+13' },\n    { name: 'Honolulu', timezone: 'Pacific/Honolulu', offset: 'UTC-10' },\n    { name: 'Anchorage', timezone: 'America/Anchorage', offset: 'UTC-9/-8' },\n    { name: 'Bangkok', timezone: 'Asia/Bangkok', offset: 'UTC+7' },\n    { name: 'Bangalore', timezone: 'Asia/Kolkata', offset: 'UTC+5:30' },\n    { name: 'Xangai', timezone: 'Asia/Shanghai', offset: 'UTC+8' },\n    { name: 'Seul', timezone: 'Asia/Seoul', offset: 'UTC+9' },\n    { name: 'Jakarta', timezone: 'Asia/Jakarta', offset: 'UTC+7' },\n    { name: 'Bangcoc', timezone: 'Asia/Bangkok', offset: 'UTC+7' },\n    { name: 'Cairo', timezone: 'Africa/Cairo', offset: 'UTC+2' },\n    { name: 'Johannesburgo', timezone: 'Africa/Johannesburg', offset: 'UTC+2' },\n    { name: 'Lagos', timezone: 'Africa/Lagos', offset: 'UTC+1' },\n    { name: 'Nairobi', timezone: 'Africa/Nairobi', offset: 'UTC+3' },\n];\n\nconst DEFAULT_TIMEZONES = [\n    'America/New_York',\n    'America/Los_Angeles',\n    'Europe/London',\n    'Asia/Tokyo',\n    'Australia/Sydney',\n    'America/Sao_Paulo'\n];\n\n// ===== CLOCK MANAGER =====\nclass ClockManager {\n    constructor() {\n        this.selectedTimezones = this.loadTimezones() || DEFAULT_TIMEZONES;\n        this.is24HourFormat = this.load24HourFormat() !== false;\n        this.init();\n    }\n\n    init() {\n        this.setupEventListeners();\n        this.renderClocks();\n        this.startUpdateCycle();\n    }\n\n    setupEventListeners() {\n        document.getElementById('add-timezone').addEventListener('click', () => this.showTimezoneModal());\n        document.getElementById('reset-timezones').addEventListener('click', () => this.resetToDefault());\n        document.getElementById('close-modal').addEventListener('click', () => this.closeModal());\n        document.getElementById('toggle-format').addEventListener('change', (e) => {\n            this.is24HourFormat = e.target.checked;\n            this.save24HourFormat();\n            this.renderClocks();\n        });\n\n        // Close modal when clicking outside\n        window.addEventListener('click', (e) => {\n            const modal = document.getElementById('timezone-modal');\n            if (e.target === modal) {\n                this.closeModal();\n            }\n        });\n    }\n\n    showTimezoneModal() {\n        const modal = document.getElementById('timezone-modal');\n        const timezoneList = document.getElementById('timezone-list');\n\n        timezoneList.innerHTML = '';\n\n        TIMEZONES.forEach(tz => {\n            const option = document.createElement('div');\n            option.className = 'timezone-option';\n            option.innerHTML = `\n                <span>\n                    <div class=\"timezone-option-name\">${tz.name}</div>\n                    <div class=\"timezone-option-offset\">${tz.timezone}</div>\n                </span>\n                <div class=\"timezone-option-offset\">${tz.offset}</div>\n            `;\n            option.addEventListener('click', () => this.addTimezone(tz.timezone));\n            timezoneList.appendChild(option);\n        });\n\n        modal.classList.add('show');\n    }\n\n    closeModal() {\n        document.getElementById('timezone-modal').classList.remove('show');\n    }\n\n    addTimezone(timezone) {\n        if (!this.selectedTimezones.includes(timezone)) {\n            this.selectedTimezones.push(timezone);\n            this.saveTimezones();\n            this.renderClocks();\n        }\n        this.closeModal();\n    }\n\n    removeTimezone(timezone) {\n        this.selectedTimezones = this.selectedTimezones.filter(tz => tz !== timezone);\n        this.saveTimezones();\n        this.renderClocks();\n    }\n\n    resetToDefault() {\n        if (confirm('Deseja restaurar os fusos horários padrão?')) {\n            this.selectedTimezones = [...DEFAULT_TIMEZONES];\n            this.saveTimezones();\n            this.renderClocks();\n        }\n    }\n\n    renderClocks() {\n        const grid = document.getElementById('timezones-grid');\n\n        if (this.selectedTimezones.length === 0) {\n            grid.innerHTML = `\n                <div class=\"empty-state\">\n                    <div class=\"empty-state-icon\">🕐</div>\n                    <div class=\"empty-state-text\">Nenhum fuso horário selecionado</div>\n                    <div class=\"empty-state-subtext\">Clique em \"+ Adicionar Fuso Horário\" para começar</div>\n                </div>\n            `;\n            return;\n        }\n\n        grid.innerHTML = this.selectedTimezones.map(timezone => this.createClockCard(timezone)).join('');\n\n        // Add event listeners to remove buttons\n        document.querySelectorAll('.remove-btn').forEach(btn => {\n            btn.addEventListener('click', () => {\n                const timezone = btn.dataset.timezone;\n                this.removeTimezone(timezone);\n            });\n        });\n    }\n\n    createClockCard(timezone) {\n        const time = this.getTimeInTimezone(timezone);\n        const tzData = TIMEZONES.find(tz => tz.timezone === timezone);\n        const name = tzData ? tzData.name : timezone;\n        const offset = tzData ? tzData.offset : 'UTC';\n\n        const timeString = this.formatTime(time);\n        const dateString = this.formatDate(time);\n        const dayOfWeek = this.getDayOfWeek(time);\n        const period = time.getHours() >= 12 ? 'PM' : 'AM';\n\n        return `\n            <div class=\"clock-card\">\n                <div class=\"timezone-name\">${name}</div>\n                <div class=\"timezone-offset\">${offset}</div>\n                <div class=\"clock-display\">\n                    <div class=\"time\">${timeString}</div>\n                    <div class=\"date\">${dateString}</div>\n                </div>\n                <div class=\"clock-details\">\n                    <div class=\"detail-item\">\n                        <div class=\"detail-label\">DIA</div>\n                        <div class=\"detail-value\">${dayOfWeek}</div>\n                    </div>\n                    <div class=\"detail-item\">\n                        <div class=\"detail-label\">PERÍODO</div>\n                        <div class=\"detail-value\">${period}</div>\n                    </div>\n                </div>\n                <button class=\"remove-btn\" data-timezone=\"${timezone}\">✕ Remover</button>\n            </div>\n        `;\n    }\n\n    getTimeInTimezone(timezone) {\n        try {\n            const formatter = new Intl.DateTimeFormat('pt-BR', {\n                timeZone: timezone,\n                year: 'numeric',\n                month: '2-digit',\n                day: '2-digit',\n                hour: '2-digit',\n                minute: '2-digit',\n                second: '2-digit',\n                hour12: false\n            });\n\n            const parts = formatter.formatToParts(new Date());\n            const time = new Date();\n\n            const hour = parseInt(parts.find(p => p.type === 'hour').value);\n            const minute = parseInt(parts.find(p => p.type === 'minute').value);\n            const second = parseInt(parts.find(p => p.type === 'second').value);\n\n            time.setHours(hour, minute, second, 0);\n            return time;\n        } catch (e) {\n            console.error('Timezone error:', e);\n            return new Date();\n        }\n    }\n\n    formatTime(time) {\n        const hours = String(time.getHours()).padStart(2, '0');\n        const minutes = String(time.getMinutes()).padStart(2, '0');\n        const seconds = String(time.getSeconds()).padStart(2, '0');\n\n        if (this.is24HourFormat) {\n            return `${hours}:${minutes}:${seconds}`;\n        } else {\n            const h = time.getHours() % 12 || 12;\n            const hStr = String(h).padStart(2, '0');\n            return `${hStr}:${minutes}:${seconds}`;\n        }\n    }\n\n    formatDate(time) {\n        const options = { year: 'numeric', month: 'long', day: 'numeric' };\n        return time.toLocaleDateString('pt-BR', options);\n    }\n\n    getDayOfWeek(time) {\n        const days = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sab'];\n        return days[time.getDay()];\n    }\n\n    startUpdateCycle() {\n        setInterval(() => this.updateClocks(), 1000);\n    }\n\n    updateClocks() {\n        document.querySelectorAll('.clock-card').forEach(card => {\n            const timezone = card.querySelector('.remove-btn').dataset.timezone;\n            const time = this.getTimeInTimezone(timezone);\n            const timeString = this.formatTime(time);\n            const dateString = this.formatDate(time);\n            const dayOfWeek = this.getDayOfWeek(time);\n            const period = time.getHours() >= 12 ? 'PM' : 'AM';\n\n            card.querySelector('.time').textContent = timeString;\n            card.querySelector('.date').textContent = dateString;\n            card.querySelectorAll('.detail-value')[0].textContent = dayOfWeek;\n            card.querySelectorAll('.detail-value')[1].textContent = period;\n        });\n    }\n\n    // ===== LOCAL STORAGE =====\n    saveTimezones() {\n        localStorage.setItem('selectedTimezones', JSON.stringify(this.selectedTimezones));\n    }\n\n    loadTimezones() {\n        const stored = localStorage.getItem('selectedTimezones');\n        return stored ? JSON.parse(stored) : null;\n    }\n\n    save24HourFormat() {\n        localStorage.setItem('is24HourFormat', JSON.stringify(this.is24HourFormat));\n    }\n\n    load24HourFormat() {\n        const stored = localStorage.getItem('is24HourFormat');\n        return stored ? JSON.parse(stored) : true;\n    }\n}\n\n// ===== INITIALIZE =====\ndocument.addEventListener('DOMContentLoaded', () => {\n    const clockManager = new ClockManager();\n    window.clockManager = clockManager;\n});\n"